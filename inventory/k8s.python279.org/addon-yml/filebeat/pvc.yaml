---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filebeat-data
  namespace: monitoring
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Mi
